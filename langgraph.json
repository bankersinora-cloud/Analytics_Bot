{
  "version": "1.0",
  "graphs": {
    "taabi-server": {
      "entrypoint": "server:mcp",
      "description": "TAABI Multi-Agent Knowledge + MCP Server",
      "env": {
        "OPENAI_API_KEY": "{{ env.OPENAI_API_KEY }}",
        "HUGGINGFACE_HUB_TOKEN": "{{ env.HUGGINGFACE_HUB_TOKEN }}"
      }
    }
  }
}
